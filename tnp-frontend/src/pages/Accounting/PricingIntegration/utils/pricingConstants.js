import { PERFORMANCE_CONFIG } from "../../config/performanceConfig";

export const DEFAULT_QUOTATION_NOTES =
  "**ไม่สามารถหักภาษี ณ ที่จ่ายได้ เนื่องจากเป็นการซื้อมาขายไป**\nมัดจำ50%ก่อนเริ่มงาน ชำระ50%ส่วนหลังก่อนส่งสินค้า";

export const DUE_DATE_OFFSET_DAYS = 30;

export const MAX_PR_FETCH = 1000;

export const DEFAULT_PRICING_PAGE_SIZE = PERFORMANCE_CONFIG.DEFAULT_PAGE_SIZE;

export const DEFAULT_DEPOSIT_PERCENTAGE = 50;

export const DEFAULT_VAT_PERCENTAGE = 7;
